services:
  mysql:
    image: mysql:8.4
    container_name: opencardhub-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: opencardhub
      MYSQL_DATABASE: opencardhub
      MYSQL_USER: opencardhub
      MYSQL_PASSWORD: opencardhub
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql

  redis:
    image: redis:7-alpine
    container_name: opencardhub-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

volumes:
  mysql-data:
  redis-data:
