{
  "name": "opencardhub",
  "version": "0.1.0",
  "private": true,
  "description": "Configurable multi-tenant virtual goods distribution platform.",
  "workspaces": [
    "server",
    "admin"
  ],
  "scripts": {
    "dev": "npm run dev --workspace server",
    "dev:server": "npm run dev --workspace server",
    "dev:admin": "npm run dev --workspace admin",
    "build": "npm run build --workspace server && npm run build --workspace admin",
    "lint": "npm run lint --workspace server && npm run lint --workspace admin"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  }
}
